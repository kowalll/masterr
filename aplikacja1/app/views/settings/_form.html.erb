<%= form_for(@setting) do |f| %>
  <% if @setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@setting.errors.count, "error") %> prohibited this setting from being saved:</h2>

      <ul>
      <% @setting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
   <div class="field">
    <%= f.label :order_title %><br>
    <%= f.select(:order_title,  ['asc', 'desc'])%>
  </div>
  <div class="field">
    <%= f.label :show_order_then   %><br>
    <%= f.text_field :show_order_then, class: "datepicker" %>
  </div>
  <div class="field">
    <%= f.label :show_active %><br>
    <%= f.check_box :show_active %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
